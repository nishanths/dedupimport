package pkg

// TODO: this test case exists to capture current behavior; it's not ideal.
// ideally we would want to remove the blank line where 'mb "math/bits"' was.

import (
	"math/bits"
	mb "math/bits"
	"json/encoding"
)