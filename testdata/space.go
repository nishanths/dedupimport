package pkg

// TODO: this test case exists to capture current behavior; it's not ideal.
// ideally we would want to remove the blank line where 'bits "math/bits"'
// was in the output.

import (
	"math/bits"
	mb "math/bits"
	"json/encoding"
)